pipeline {
    agent any

    environment {
        VENV = 'venv'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Ummekulsum320/job_portal_backend'
            }
        }

        stage('Setup Python Env') {
            steps {
                sh '''
                python3 -m venv ${VENV}
                . ${VENV}/bin/activate
                pip install --upgrade pip
                pip install -r requirements.txt
                '''
            }
        }

        stage('Run Migrations') {
            steps {
                sh '''
                . ${VENV}/bin/activate
                python manage.py migrate
                '''
            }
        }

        stage('Run Tests') {
            steps {
                sh '''
                . ${VENV}/bin/activate
                # Start Django server in background
                nohup python manage.py runserver 127.0.0.1:8000 > server.log 2>&1 &
                sleep 5  # wait for the server to start

                echo "✅ Server started, running check.py now..."
                python check.py
                '''
            }
        }
    }

    post {
        success {
            echo '✅ Pipeline completed successfully!'
            mail to: 'your_email@example.com',
                 subject: 'Job Portal Backend - Build Success',
                 body: 'The Jenkins build succeeded 🎉.'
        }

        failure {
            echo '❌ Pipeline failed, sending error email...'
            mail to: 'your_email@example.com',
                 subject: 'Job Portal Backend - Build Failed ❌',
                 body: 'The Jenkins build has failed. Please check the logs.'
        }
    }
}